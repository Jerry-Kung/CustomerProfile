[project]
name = "customer-profile"
version = "0.2.0"
description = "潜在目标客户人设画像分析工作流（Dify 迁移后的 Python 实现）"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-q"
pythonpath = ["src"]
