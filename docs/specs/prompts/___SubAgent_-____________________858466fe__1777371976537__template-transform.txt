**任务背景**：
我是一名汽车销售顾问，刚刚认识了一位可能有购车意向的潜在客户。我从公开的合法渠道获取了该客户的一些信息（例如微信主页，朋友圈截图等），并为这位客户构建了一幅“个人特征画像”。现在，我需要基于客户的画像数据，为该客户生成3-5个最精准的人设标签，用于帮助其他销售人员能够快速了解这位客户。

**任务目标及要求**：
我已经通过专家系统基于该客户的基础信息生成了与该客户适配的10-20个候选标签（包含生成标签的推理过程，及每个标签相关联的客户信息证据线索）。你的任务是把这10-20个标签整合浓缩到3-5个，相应的参考证据/推理过程也请一并合并。
原始的10-20个候选标签为标签库中抽出来的固定化标签，你在整合过程中不必保证最终的标签名称与候选标签完全一致，可以根据实际情况对标签内容进行合理调整。

对合并后的3-5个标签的要求：
- 最符合当前客户的人设画像
- 描述性/画面感强，能够让人一眼看上去就能建立对客户整体形象的大概认知
- 尽可能属于不同的类型，以便全面描述该客户
- 用词精致、潮流、时髦，每个标签不超过8个字
- 避免使用包含地域、职业、年龄、收入等词汇的用语，这类用语容易产生侵犯隐私感，且一旦出现错误容易严重误导销售人员

**任务输入**：
- 基于客户的公开信息整理的客户证据线索列表，格式如下：
{{ output }}

- 客户的特征画像（包含人设特征、兴趣爱好特征和消费性格特征三个部分），格式如下：
{{ output_1 }}

- 根据客户的信息，为客户预选择过的10-20个初始标签。

**任务输出**：
请采用JSON格式输出你的结果（及推理过程）。
JSON格式说明：
{
	// 标签列表，3-5个
    "inferred_tags": [
        {
            "id": "tag_001",
				
	        // 标签名称
            "title": "二孩硬核奶爸",
				
		    // 标签描述
            "desc": "家庭责任感重，购车完全围绕伴侣与子女的需求展开",
				
		    // 本条推理的置信度，0~1
            "confidence": 0.98,
				
			// 标签icon
            "icon": "family_man",
				
			// 点击标签后展开的推理详情
            "reasoning_detail": {
				 // 对本条推理的简短总结
                "summary": "基于子女数量与高频家庭行为推导",
					
				// 推理步骤
                "steps": [
                    "确认客户目前育有两个未成年子女",
                    "周末存在高频长时的亲子陪伴与户外活动",
                    "购车首要动因是解决带娃出行的装载与舒适度痛点"
                ],
					
				// 本条推理的置信度，0~1
                "confidence": 0.98,
					
				// 支撑该推理的证据锚点
                //【**重要**】**请使用自然语言描述证据内容，保证内容可直接阅读，坚决禁止使用证据ID（例如ev_001）、JSON类原始数据字段（例如locked_evidence）描述证据锚点**
                // 【**重要**】**证据锚点中的内容均需直接来源于客户的原始证据线索，禁止使用“画像”、“人设”、“分析”类主观推断内容作为证据锚点，需要引用上述主观推断内容时，需要提取其关联的原始证据线索作为证据锚点**
                "evidence": [
                    "录音：大宝马上上小学，二胎刚半岁",
                    "图片：周末带娃放电",
                    "录音：现在的轿车根本装不下婴儿车"
                ]
            }
        },
		...
    ]
}

**注意事项**：
- 请使用简体中文输出。
- 请直接输出纯 JSON，不要使用 ```json 或 ``` 包裹，不要输出任何解释文字。请保证你的输出能够被json.loads()直接解析，不得包含任何多余的内容或字符，各Json字段的value内容请使用简体中文填写。

输入内容：
{{ output_2 }}
【预生成的客户标签】：
{{ text }}
请给出你的输出：