# 差异清单 v2（V0.3）

> 依据：`docs/specs/V0迁移规划.md`（§2 待决问题、§6 工程优化规则）、`Dify迁移任务说明.md`（§3.1/§3.2/§6/§7.2）。
> v2 变更（V0.3）：§2.2 的 P1–P4 与 §2.3 的 M1 由「待确认」改为**实测复核已完成**（依据是 V0.3 的执行器与测试，见 §5）；新增 §1.1 的 V0.3 有意差异。
> 每条只标注三种状态之一：**已确认**（已决策或有明确外部结论）、**待确认**（缺外部输入）、**缺省采用**（无结论时按既定缺省执行，并已记录）。
> 无空白项。凡实测与文档不符者，一律以本清单记录的实测值为准。

## 0. 本轮实测修正（与规划文档不一致，以实测为准）

| 编号 | 规划文档说法 | 实测结果 | 处置 |
|---|---|---|---|
| D1 | 「4 处 HTTP 节点的 header 含明文 `X-API-Key`」（§1.3-9、§2.5） | **5 个节点、2 个密钥值** | 已确认。详见 `secrets_report.md` |
| D2 | `phone_numbers.txt` 为「0 字节空文件」（§2.2 Q9） | 含 **20 个真实手机号**，258 字节 | 已确认。V0.2 起的验证输入源已就绪 |
| D3 | 节点/边总数 293/324（§1.1） | 293/324，**逐类型计数亦全部一致** | 已确认。16 项断言全部命中（`extract_ledger.py` 的 cross_check） |
| D4 | 1 个聚合器开启 `group_enabled`（§1.3-4） | 1 个，位于 `人设特征判断` 节点 `1776760747042`，分组 `hobby` / `consumption`；**字段位于 `advanced_settings` 之下，非节点顶层** | 已确认（实现口径） |
| D5 | 仓库根目录 `Untitled` 文件 | 受版本控制的空壳文件，内容仅一行 `dify_dsl_data`，疑为误提交的临时文件 | **已删除**（用户 2026-09-23 决定，见 §4） |

## 1. 有意差异（已决策，非缺陷）

| 编号 | 差异 | 依据 | 状态 |
|---|---|---|---|
| W1 | 入口图删除冗余节点 `1777361789855` 及其 2 条边（`1777278748069 → 1777361789855 → end`）。入口图由 6 节点/6 边降为 **5 节点/4 边** | Q3 答复「只保留一个节点，不执行两次」 | 已确认 |
| W2 | 17 处 `gemini_retry_2_times` 调用点（`tool` 节点）与 `Gemini（异常输出重试版）` 子流程不再作为独立图存在，归一为一次 `llm_call()` | §6.3 | 已确认 |
| W3 | 重试机制下沉到 `llm_call()` 内部（最多 3 次尝试），等价性口径由「图形态一致」改为「行为一致」 | §6.3、§6.4.2 | 已确认 |
| W4 | 8 个工作流 `start` 的 `llm_model` 入参删除；4 个相关 `if-else` 节点归一（`人设特征判断` 2 处、`外呼录音信息提取`、`试驾录音信息提取`），任走一支 | §6.4.5 | 已确认 |
| W5 | 三套模型标识（通义 `qwen3.8-flash` 32 处、火山 `doubao-seed-2-0-pro-260215` 2 处、`doubao-seed-2-0-lite-260215` 1 处）收敛为 `.env` 中单一 `LLM_MODEL` | §6.4.4 | 已确认 |
| W6 | 四档 temperature（0.2 二十处、0.4 八处、0.7 三处、1 四处）全部归一为 **0.5**，thinking 开启，不配其他参数 | §6.4.4 | 已确认 |
| W7 | 迭代 / 聚合 / 条件节点折叠为普通控制流（`for` 循环、赋值/三元、`if/elif/else`） | §6.2 | 已确认 |
| W8 | 图节点/边数将少于 293/324 | §6.7 | 已确认（具体数字见 §3） |

## 1.1 V0.3 新增的有意差异

| 编号 | 差异 | 依据 | 状态 |
|---|---|---|---|
| W9 | 传给子流程的 `llm_model` **字面量**（`"gemini"`）一并删除，不只是 `start` 的入参。原值是固定串，属同一处废弃参数的残留 | §6.4.5 | 已确认 |
| W10 | 新增 `WRITEBACK_ENABLED` 开关，**缺省 `false`**。置 false 时回写节点不发请求，只产出「已跳过」的可解释结果 | §9.3、Q1/Q2 缺省处理 | 已确认。真实冒烟亦保持 false——V0.3 的验收目标是端到端**读**通 |
| W11 | 提示词正文的运行期唯一来源是包内 `src/customer_profile/templates/`；`docs/specs/prompts/` 降为**评审副本**，由测试断言两边逐字符一致 | D8、`TemplateRepository.DEFAULT_TEMPLATE_DIR` | 已确认。避免同一正文存在两个事实来源 |
| W12 | `tool` 节点的 `@inputs` 支持 `@literal:<值>` 前缀，承载 DSL 里子流程入参的字面量（典型为截图类工作流的 `data_source`） | 迁移实测 | 已确认 |
| W13 | 迭代节点的「收集哪个字段」以 `@output` **绑定**声明，不使用 `config["output"]`（后者是聚合结果的字段名）。校验层对迭代节点的 `@output` 指向循环体节点作豁免 | 迁移实测 | 已确认 |
| W14 | AUC 语音识别由**内网自建服务**改为**火山引擎云服务**（`openspeech.bytedance.com`）：云服务异步提交 + 由调用方轮询、状态在响应头`X-Api-Status-Code`、`task_id` 由调用方生成，与 DSL 契约不同。`src/customer_profile/execution/auc.py` 做协议适配，**保持**`/submit_analyze` 与 `/query_analyze` 两个节点及其下游 code 节点不变 |用户 2026-09-23 决定（内网服务长期不可达） | 已确认。真实录音冒烟通过，见`v0.3_smoke_report.md` |

## 1.2 V0.4 新增的有意差异

| 编号 | 差异 | 依据 | 状态 |
|---|---|---|---|
| W15 | **布局改为自动计算，放弃复用 DSL 坐标。** `NodeDef.coords` 字段保留（有值时优先），但 285 个节点中仅 22 个有值，不再回填 | 用户 2026-09-23 决定：回填会产生一份与 DSL 强相关的派生物，与「纯代码构建、与 Dify 解关联」的初衷相悖 | 已确认。算法见 `V0.4只读运行台.md` §2.2，测试见 `tests/test_layout.py` |
| W16 | **前端构建产物 `frontend/dist/` 入库。** 仓库根 `.gitignore` 与 `frontend/.gitignore` 各加例外 | `V0.4只读运行台.md` §4：运行台要能在未安装 node 的机器上直接启动 | 已确认。代价是改前端必须重新构建并提交产物，见 `frontend/README.md` |
| W17 | `SERVE_UI` 缺省 **false**：不托管前端时服务照常启动；置 true 但未构建时打印告警并跳过，**不**启动失败 | 「可选功能不应变成硬依赖」，同 W10 的取向 | 已确认 |
| W18 | 设计文档初稿称「迭代节点带环」，实测 **20 个定义全部无环**；布局按 DAG 设计，不实现断环语义 | 实现时实测（`structural_order()` 逐个成功） | 已确认。环普查由 `tests/test_layout.py::test_no_real_workflow_is_cyclic` 断言 |

## 1.3 V0.5 新增的有意差异（v0.5.1）

| 编号 | 差异 | 依据 | 状态 |
|---|---|---|---|
| W19 | **V0.5 只做工作台任务触发入口，「生产运行保障」整体后移。** 原 V0.5 交付物（持久化队列 + 独立 worker、重启中断处理入口、全局配额、幂等回写、影子运行、运维说明）不在本次范围 | 用户 2026-09-24 决定；Q2/Q7/Q8 三个硬缺口仍未关闭，按「缺省采用」推进 | 已确认。设计见 `docs/specs/V0.5工作台任务触发入口.md` |
| W20 | **`MAX_ACTIVE_RUNS` 由死配置改为真正生效**：此前只声明、全仓无读取方；现于服务层装 `RunSlotGate`，满则**拒绝**（HTTP 429）而非排队。子运行不占该名额 | 工作台一开提交入口，重复提交会堆叠真实模型调用；「先落库再返回」的契约不应被无限并发撑破 | 已确认。`tests/test_submit_gate.py` |
| W21 | **主入口手机号在提交前校验**（`^1[3-9]\d{9}$`，422），**只对 `customer_profile_entry` 生效** | 此前无任何前置校验，少传/传错会变成失败运行而非可解释的 4xx；手机号还会进下游 HTTP 路径 | 已确认。同测试文件 |
| W22 | **新增只读端点 `GET /runtime`**，报出回放模式、**回写开关**、模型名与并发上限。**不返回任何凭据** | `WRITEBACK_ENABLED=false` 时回写被跳过而运行仍 succeeded；不在界面上显式标出，会被读成「已写进生产库」 | 已确认。同测试文件 |

## 1.4 V0.5.2 新增的有意差异（持久化队列 + 独立 worker）

| 编号 | 差异 | 依据 | 状态 |
|---|---|---|---|
| W23 | **执行从 API 进程移到 worker；`POST /runs` 的 429 触发条件由「运行名额满」改为「队列深度超 `MAX_QUEUED_RUNS`」。** `RunSlotGate` 保留但只作用于进程内路径（`Service.submit_local`） | 用户 2026-09-24 决定「队满则拒」。队列模型下执行已不在提交侧，API 无从同步判断「运行名额」；`Dify迁移任务说明.md` §4 要求 API 与 worker 分离 | 已确认。`tests/test_worker_queue.py`、`tests/test_submit_gate.py` |
| W24 | **`mark_running_as_interrupted` 只标 `running`，不再标 `queued`。** 此前两者一并标为中断 | queued 是持久化队列里等待被领取的任务；一并标成中断会让「worker 重启不丢任务」这条验收标准直接不成立 | 已确认。`tests/test_persistence.py::test_interrupted_runs_are_marked_on_startup` |
| W25 | **新增最小补列机制**（`_apply_migrations`，读 `PRAGMA table_info` 后按需 `ALTER TABLE ADD COLUMN`），给既有库补 `queued_at_ms` / `claimed_at_ms` / `claimed_by` / `lease_expires_at_ms` 四列 | `CREATE TABLE IF NOT EXISTS` 对已存在的表不会补列，而 `data/customer_profile.db` 已在磁盘上；不补列则新列在运行期只表现为一句 `no such column`。**不引入 alembic** 等迁移框架（规划 §5 约束 7） | 已确认。`tests/test_worker_queue.py::test_migration_adds_missing_columns_to_an_existing_db` |
| W26 | **队列复用 `workflow_runs` 表，不新建队列表。** 入队即写一行 `status='queued'`，队列相关列全部可空 | 用户 2026-09-24 决定。另建表会让「运行」有两个事实来源与跨表状态同步；代价是留痕表兼任队列 | 已确认 |
| W27 | **保留 `INLINE_WORKER`（缺省 true）：API 进程同时充当一个消费者。** 生产按 §4 分离部署时置 false | 五个既有测试文件（`test_api.py` 等）依赖「POST /runs 后立刻能查到并跑完」的契约；单机部署也不应被迫另起进程。分离能力由配置提供，不靠删代码 | 已确认。`tests/test_worker_queue.py::test_inline_worker_is_off_when_disabled` |
| W28 | **租约过期不自动重领**，`Store.requeue_expired_leases` 保留为显式动作 | 重领会把跑到一半的运行**整条重跑**；根目录文档明确「不做任意节点断点自动续跑」，且 worker 停机不证明外部副作用未发生。幂等回写（V0.5.5）落地前，自动重跑会重复触发外部副作用 | 已确认。见 `worker.py` 的启动恢复注释 |

## 1.5 V0.5.3 新增的有意差异（全局配额与限额）

| 编号 | 差异 | 依据 | 状态 |
|---|---|---|---|
| W29 | **`LLM_RPM_LIMIT` / `LLM_TPM_LIMIT` 由死配置改为真正生效。** 两者自 V0.2 起声明在 `Settings` 里、`.env.example` 写着「V0.5 起生效」，但**全仓无任何读取方**——与 `MAX_ACTIVE_RUNS` 在 v0.5.1 之前是同一形态 | 规划 §3 V0.5 交付物 3「模型侧 RPM/TPM 约束」；Q8 无明确数值，故走保守默认 + 配置化 | 已确认。`tests/test_quota.py` |
| W30 | **限额用令牌桶而非固定窗口。** 令牌桶容量由 `LLM_RPM_BURST` / `LLM_TPM_BURST` 配置，置空时取对应 limit | 固定窗口在边界会瞬时放行 2× 配额；本项目一次运行 32 次 LLM 调用、历时二十余分钟，恰好会撞在边界上 | 已确认。桶的等待时长由测试逐秒断言 |
| W31 | **限额接线在「每次真实出网」那一层**（`_single_attempt`），不在 `llm_call` 的循环外层 | 放在循环外层会让 3 次尝试只记 1 个请求，配额被系统性低估 3 倍 | 已确认。`tests/test_quota.py::test_rpm_waits_after_the_bucket_is_drained` |
| W32 | **TPM 用「出网前按 prompt 长度预扣 + 拿到 usage 后校正」。** `usage` 缺失时**不校正** | 不预扣则无从在出网前限制；不校正则估算偏差在长会话下持续累积；而「未知」不能当成 0 退还，那会凭空放大额度 | 已确认。2 项测试分别钉住校正与不校正 |
| W33 | **回放模式不消耗额度** | 回放请求没有真的发出去，让它排队会让全量测试凭空变慢，还会掩盖真实限额行为 | 已确认 |
| W34 | **单次需求超过桶容量时显式报错，不死等** | 这是配置问题（burst 配得比单次请求还小），死等会让运行永久卡住且看不出原因 | 已确认 |
| W35 | **多 worker 配额为「显式分配」：`LLM_RPM_LIMIT` 是全局额度，按 `WORKER_REPLICAS` 等分给每进程。** 不可整除时**启动即报错** | 规划 §3 V0.5 验收要求「配额在多 worker 场景下语义明确」；不校验则 RPM=60 配 4 个 worker 会静默打到 240 | 已确认。`test_limits_must_divide_evenly_across_workers` |
| W36 | **不做跨进程共享配额。** 限额器是进程内的 | 共享需要中间件，与规划 §5 约束 7「不引入未实际需要的重型框架」冲突；且当前部署形态是单 worker（`Dify迁移任务说明.md` §4）| 已确认 |
| W37 | **并发上限与速率限额并存，不合并。** `MAX_CONCURRENT_REQUESTS` 限「同时打出去几个」，RPM/TPM 限「一分钟总共几个」 | 两者是不同维度，合并会丢掉一个 | 已确认。二者在 `_single_attempt` 内相邻生效：**先速率、后并发**（速率等待可达数十秒，先占并发名额会把其它可出网的请求一起堵住）|

## 1.6 V0.5.4 新增的有意差异（中断处理与人工出口）

| 编号 | 差异 | 依据 | 状态 |
|---|---|---|---|
| W38 | **新增人工出口 `POST /runs/{run_id}/abandon`**，把 `interrupted` 运行终结为 `cancelled`。**只接受 `interrupted`**，其它状态 409 | v0.5.2 已把「残留 running 标 interrupted 并保留已完成结果」做对，但无任何出口——这些运行会永远留在列表里，看不出是「待处理」还是「已处理」 | 已确认。`tests/test_interrupt.py` |
| W39 | **终结用条件 UPDATE（`WHERE status='interrupted'`），不是「先查后写」** | 先查后写之间存在窗口，窗口内该运行可能已被另一个 worker 重新领取并开始执行；此时改状态会与一个正在跑的运行冲突，而界面上看不到任何在跑的东西 | 已确认。`test_abandon_only_accepts_interrupted` |
| W40 | **终结时 `error` 为追加而非覆盖**，并补 `finished_at_ms` | 原中断原因才是「为什么停下」的答案；覆盖掉之后人工回看无从判断 | 已确认。`test_abandon_preserves_the_interrupt_reason` |
| W41 | **`/abandon` 与 `/cancel` 语义分离**：前者结掉已停的运行，后者停止正在跑的运行 | 混用会出现「点了取消但任务还在写外部接口」——界面上已是终态，进程里还在跑 | 已确认。两者各自 409 条件不同 |
| W42 | **`docs/architecture.md` 首次填充**（此前 0 字节）。D6 曾决定「待架构设计阶段编写」，本版即该阶段 | `docs/README.md` 为它定义了收录标准（系统边界、模块职责、分层、数据流、外部依赖、技术约束）；空文件会让人误以为「已写过」 | 已确认 |
| W43 | **`docs/adr/` 与 `docs/runbooks/` 首次落地**（此前三目录全空） | 同上；ADR 收录不易从代码看出的决策（队列为何不用 Redis、配额为何显式分配），runbook 收录部署与故障定位 | 已确认 |

## 2. 未知运行语义（DSL 无法确认）

### 2.1 外部接口与协议

> **用户 2026-09-23 决定：以下仍无外部结论的问题，一律按「缺省采用」执行**（即右列的缺省处理即为实现依据）。状态列保留「待确认」以标记结论来源不是外部输入，避免日后误认为是已核实的协议。

| 编号 | 问题 | 状态 | 缺省处理 |
|---|---|---|---|
| Q1 | 回写接口 `POST /callback/update-profile` 的成功判定协议？HTTP 200 是否等同业务成功？ | **已确认**（用户 2026-09-24 答复：**2xx 即业务成功**）。**落地顺延**至 V0.5.5（幂等回写随影子运行一并后移） | 现实现仍为原缺省：只记 HTTP 状态与响应体。V0.5.5 改为「2xx 记成功并留响应体为证」 |
| Q2 | 回写接口是否支持幂等键？粒度为何？ | **已确认**（用户 2026-09-24 答复：**支持幂等键，粒度 = 客户**）。**落地顺延**至 V0.5.5 | 现实现仍为原缺省：不自动重试 POST。V0.5.5 改为「同客户复用同一键，重试安全」 |
| Q5 | HTTP 的 `timeout: 0`、`retry_interval`（无单位）、`max_retries: 3` 的实际运行语义 | **缺省采用**（阻塞 V0.2） | 采用显式配置默认值（连接 10s / 读 600s、间隔 500ms），见 `.env.example` §5。**不得把 DSL 原值直接传给 HTTP 库** |
| Q6 | 语音服务轮询的终止条件与最大轮询次数（`poll_count` 只被提取未被使用） | **缺省采用**（阻塞 V0.3） | 按有限次数上限实现，上限值标注待确认 |
| Q7 | 现有调用方协议：同步/异步、超时、鉴权、字段结构 | **缺省采用**（阻塞 V0.5） | 先只提供异步提交 + 查询 run_id |
| Q8 | 峰值请求率、单任务模型调用数与 token 量、期望时延、留痕保留期 | **缺省采用**（阻塞 V0.5） | 先设保守默认值，配置化 |
| ~~Q4~~ | ~~`llm_model` 合法取值~~ | **已关闭** | 由 §6.4.5 解决（参数废弃） |
| ~~Q3~~ | ~~入口图双节点是否并行重复调用~~ | **已确认** | 只保留一个节点（见 W1） |
| ~~Q9~~ | ~~回放验证的输入来源~~ | **已确认** | `phone_numbers.txt`（实测非空，见 D2） |
| Q7 | 现有调用方协议：同步/异步、超时、鉴权、字段结构 | **已确认**（用户 2026-09-24 答复：**沿用当前 API 层**）。注意 V0.5.2 改了 `POST /runs` 的 429 触发条件（W23），属行为微调而非契约变更：入参形状与成功返回值不变 | 不改对外契约 |
| Q8 | 峰值请求率、单任务模型调用数与 token 量、期望时延、留痕保留期 | **已确认**（用户 2026-09-24 答复：**暂无明确数值**） | 继续按保守默认值 + 配置化；`MAX_QUEUED_RUNS=50` 是保守值，非压测结论 |

### 2.2 Dify 平台语义（本轮新增）

| 编号 | 问题 | 状态 | 缺省处理 |
|---|---|---|---|
| P1 | `iteration` 节点的 `parallel_nums: 10` 与 `is_parallel: false` 组合语义 | **已复核（实现为串行）** | **按串行实现**。`is_parallel: false` 是权威信号，`parallel_nums: 10` 在其下不生效。V0.3 实测：8 个迭代节点的 `is_parallel` **全为 `false`**，且逐项留痕按 `call_path` 区分（`tests/test_iteration.py`） |
| P2 | `iteration` 的 `error_handle_mode: terminated` 派生行为：某一项失败是否终止整轮并阻断下游？ | **已复核（终止即阻断）** | 实测 8 个迭代节点的 `error_handle_mode` **全为 `terminated`**。实现为「某项失败即终止整轮」，错误信息指明失败项序号，下游按既有 P2 缺省阻断；已完成项的结果保留（`tests/test_iteration.py::test_iteration_stops_on_first_failure`） |
| P3 | `variable-aggregator` 开启 `group_enabled` 时的取值行为（多组并存的输出形态） | **已复核（每组包 `{"output": 值}`）** | 返回普通对象，键为组名。**V0.3 修正**：每组的值不是裸值，而是 `{"output": 值}`——实测下游 `code` 取 `hobby_result_object["output"]`，且这些入参在 DSL 里声明为 `value_type: object`。已按此实现（`executors_aggregator.py`，`tests/test_aggregator.py::test_grouped_aggregator_wraps_each_group_in_output`） |
| P4 | `if-else` 两侧分支汇聚到同一 `variable-aggregator` 时的取值行为（非教科书式二选一，出现在人设特征判断与各截图类工作流） | **已复核（判据是「执行过」而非「非空」）** | **V0.3 修正判据**：取「第一个**真正执行过**的输入」，判据是 `ctx.has(node_id)`，**不是**「值非空」。空串是合法产出（`无数据，输出默认信息` 模板的正常结果、各截图流程 code 在未命中 channel 时明确返回空串）；按非空判定会把这些正常结果误判为缺失，静默改用另一支的值（`tests/test_aggregator.py::test_aggregator_takes_first_executed_even_when_empty`） |
| P5 | HTTP 非 2xx 是否使节点失败？插件默认参数为何？ | **已复核（≥400 即失败，可配兜底）** | 实现为 `HttpAttempt.succeeded` 要求 `status < 400`；节点可用 `@error_strategy: default-value` 改为取兜底值而不失败。超时/重试参数一律走显式配置（Q5），不把 DSL 原值传给 HTTP 库 |

### 2.3 模型与提示词

| 编号 | 问题 | 状态 | 缺省处理 |
|---|---|---|---|
| M1 | 「3 次尝试」是指最多 3 次（首次 + 2 重试），还是首次 + 3 次重试 = 4 次？ | **缺省采用（实现为最多 3 次）** | 按**最多 3 次尝试**实现（`llm.py` 的 `MAX_ATTEMPTS = 3`，§6.4.3）。**仍无外部结论**：这是缺省口径而非已核实协议，沿用 v1 的标记方式，不写成「已确认」 |
| M2 | 17 处 `tool` 节点的 `output_json` 入参**全部为 `false`**，无法据此判断任务是否需要 JSON 输出 | **已确认（事实）** | 按「下游消费方语义」逐个确认：下游 `code` 对其输出做 JSON 解析、或提示词明确约定输出 JSON，即 `expect_json=True`。须在 V0.3 逐点标注 |
| M3 | 全项目 12 个 vision 节点要求统一模型具备多模态能力 | **已确认（硬约束）** | `.env` 的 `LLM_MODEL` 选型时必须确认；实测 12 个节点全部位于 6 个截图解析工作流，每流 2 个 |

## 3. 双套计数

### 3.1 原 DSL 计数（实测，16 项断言全部命中）

| 项 | 数量 |
|---|---|
| 工作流 | 19 |
| 节点 | 293 |
| 边 | 324 |
| `tool` 节点 / 其中 `gemini_retry_2_times` | 38 / 17 |
| `llm` 节点 / 其中 vision | 35 / 12 |
| `code` | 49 |
| `template-transform` | 46 |
| `if-else` | 26 |
| `variable-aggregator` / 其中分组 | 23 / 1 |
| `end` | 22 |
| `start` | 19 |
| `http-request` | 19 |
| `iteration` / `iteration-start` | 8 / 8 |
| 绑定条目（见下方口径说明） | 329 |
| DSL 文本中 `{{#node.field#}}` 出现次数 | 111 |
| 顺序依赖但无数据消费的边 | 145 |
| 提示词/模板节点数 | 81（46 + 35） |

> **口径说明（易错点）**：「绑定条目」与「`{{#...#}}` 出现次数」是两个不同的数，不可混用。绑定条目包含节点的 `variables[]` 声明、`tool` 节点的每个参数、条件分支的变量选择器——即使其值不是引用字面量也算一条。DSL 文本中 `{{#...#}}` 的实际出现次数为 111。两者均记录在 `variable_ledger.json`。

### 3.2 §6 优化后的 Python 实现计数（**规划预算值，非已实现验证值**）

以下为按 §6 规则推算的预算，**尚未实现、未经验证**，V0.3 实现后须以实测替换：

| 差异来源 | 影响 |
|---|---|
| Q3 决策（W1） | −1 节点、−2 边 |
| 17 处 `gemini` 归一（W2/W3） | −17 个 `tool` 节点；`Gemini（异常输出重试版）` 子流程的 11 节点不再作为独立图存在 |
| 4 处 `llm_model` 分支归一（W4） | −4 个 `if-else` 节点；8 个工作流的 `start` 各减少 1 个入参 |
| 迭代/聚合/条件折叠（W7） | `iteration` 8、`iteration-start` 8、`variable-aggregator` 23、`if-else` 26 全部改为普通控制流 |
| `template-transform` 合并（§6.5） | 46 个节点可合并为同一模块的顺序代码；**提示词正文仍独立存放** |

**§3.2 不给出单一总数**：折叠后的「节点」不再是同一维度的事物（一个 `for` 循环替代 2 个节点），给出单一数字会制造虚假精度。**唯一有意义的对照基准是 §3.1 的 DSL 计数**，V0.3 实现时逐项建立「DSL 节点 → Python 函数」的对应关系并单独出具。

## 4. 已决定事项（用户 2026-09-23 答复）

| 编号 | 事项 | 决定 | 落地情况 |
|---|---|---|---|
| D5 | 仓库根目录受控文件 `Untitled` 是否删除 | **删除** | 已删除（`git rm --cached` + 删文件）。确认其内容仅一行 `dify_dsl_data`、无引用方 |
| D6 | `docs/specs/` 缺 `README.md`；`docs/architecture.md` 为空 | `architecture.md` **待后续完成代码架构设计开发后编写**；`specs/` 目录专门存放各类设计文档 | 本版本不新增 `specs/README.md`；`architecture.md` 保持为空，留待架构设计阶段 |
| D7 | 密钥轮换时机 | **在完成 V0 版本后手工执行，V0 开发阶段暂不考虑** | `secrets_report.md` §3 的「V0.3 前」建议已作废，改为 V0 完成后手工轮换。V0 全程不触达生产外网，风险窗口可控 |
| D8 | 提示词正文是否入库 | **需要入库**，是后续版本的重要优化目标 | 已由 `data/prompts/`（gitignored）迁至 **`docs/specs/prompts/`（入库）**，81 份。入库前已扫描确认不含密钥与手机号，`verify_ledger.py` 持续校验 |

### 4.1 其他待决问题的处置

用户指示：**其余待决策的 Qx 问题按缺省方案实现**。据此，§2.1 的 Q1、Q2、Q5–Q8 的状态改为「缺省采用」，右列缺省处理即为实现依据。

**注意区分**：该指示覆盖的是 **Qx 系列**（外部接口与协议）。§2.2 的 P1–P5（Dify 平台语义）与 §2.3 的 M1（重试次数口径）**不在本次指示范围内**，仍为待确认，按各自缺省处理推进，但须在 V0.2/V0.3 实现时以 Dify 实际行为或执行记录复核。

### 4.2 本清单的后续维护

- 本文件是 **v2**。v1 → v2 的实质变化是 §2.2/§2.3 的四条 Dify 语义由「待确认」转为实测复核结论，其中 **P3 与 P4 的复核改变了实现口径**（每组包 `{"output": 值}`；判据由「非空」改为「执行过」）。Q1/Q2/Q5–Q8 的「缺省采用」是**实现依据，不是等价验证通过**——缺外部协议时不得宣称等价。
- 任一 Qx 获得外部结论后，须回来更新本表并把状态改为「已确认」，同时修订实现。
- P1–P5、M1 的「已复核」指的是**与本项目实现一致且经测试固定**，不等于已向 Dify 官方接口核实。若日后发现 Dify 实际行为不同，仍须回来修订。

## 5. V0.3 复核依据

| 结论 | 依据 | 固化于 |
|---|---|---|
| P1 串行、P2 终止即阻断 | 8 个迭代节点配置实测 | `tests/test_iteration.py` |
| P3 分组包 `{"output": 值}` | 下游 code 取 `["output"]` + `value_type: object` | `tests/test_aggregator.py` |
| P4 判据为「执行过」 | 空串是合法产出的两处证据 | `tests/test_aggregator.py` |
| P5 非 2xx 失败 | `HttpAttempt.succeeded` 实现 | `tests/test_api.py` |
| M1 最多 3 次 | `MAX_ATTEMPTS` | `tests/test_llm_retry.py` |
