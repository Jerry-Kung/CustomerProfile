# 差异清单 v1（V0.1.1）

> 依据：`docs/specs/V0迁移规划.md`（§2 待决问题、§6 工程优化规则）、`Dify迁移任务说明.md`（§3.1/§3.2/§6/§7.2）。
> 每条只标注三种状态之一：**已确认**（已决策或有明确外部结论）、**待确认**（缺外部输入）、**缺省采用**（无结论时按既定缺省执行，并已记录）。
> 无空白项。凡实测与文档不符者，一律以本清单记录的实测值为准。

## 0. 本轮实测修正（与规划文档不一致，以实测为准）

| 编号 | 规划文档说法 | 实测结果 | 处置 |
|---|---|---|---|
| D1 | 「4 处 HTTP 节点的 header 含明文 `X-API-Key`」（§1.3-9、§2.5） | **5 个节点、2 个密钥值** | 已确认。详见 `secrets_report.md` |
| D2 | `phone_numbers.txt` 为「0 字节空文件」（§2.2 Q9） | 含 **20 个真实手机号**，258 字节 | 已确认。V0.2 起的验证输入源已就绪 |
| D3 | 节点/边总数 293/324（§1.1） | 293/324，**逐类型计数亦全部一致** | 已确认。16 项断言全部命中（`extract_ledger.py` 的 cross_check） |
| D4 | 1 个聚合器开启 `group_enabled`（§1.3-4） | 1 个，位于 `人设特征判断` 节点 `1776760747042`，分组 `hobby` / `consumption`；**字段位于 `advanced_settings` 之下，非节点顶层** | 已确认（实现口径） |
| D5 | 仓库根目录 `Untitled` 文件 | 受版本控制的空壳文件，内容仅一行 `dify_dsl_data`，疑为误提交的临时文件 | **已删除**（用户 2026-09-23 决定，见 §4） |

## 1. 有意差异（已决策，非缺陷）

| 编号 | 差异 | 依据 | 状态 |
|---|---|---|---|
| W1 | 入口图删除冗余节点 `1777361789855` 及其 2 条边（`1777278748069 → 1777361789855 → end`）。入口图由 6 节点/6 边降为 **5 节点/4 边** | Q3 答复「只保留一个节点，不执行两次」 | 已确认 |
| W2 | 17 处 `gemini_retry_2_times` 调用点（`tool` 节点）与 `Gemini（异常输出重试版）` 子流程不再作为独立图存在，归一为一次 `llm_call()` | §6.3 | 已确认 |
| W3 | 重试机制下沉到 `llm_call()` 内部（最多 3 次尝试），等价性口径由「图形态一致」改为「行为一致」 | §6.3、§6.4.2 | 已确认 |
| W4 | 8 个工作流 `start` 的 `llm_model` 入参删除；4 个相关 `if-else` 节点归一（`人设特征判断` 2 处、`外呼录音信息提取`、`试驾录音信息提取`），任走一支 | §6.4.5 | 已确认 |
| W5 | 三套模型标识（通义 `qwen3.8-flash` 32 处、火山 `doubao-seed-2-0-pro-260215` 2 处、`doubao-seed-2-0-lite-260215` 1 处）收敛为 `.env` 中单一 `LLM_MODEL` | §6.4.4 | 已确认 |
| W6 | 四档 temperature（0.2 二十处、0.4 八处、0.7 三处、1 四处）全部归一为 **0.5**，thinking 开启，不配其他参数 | §6.4.4 | 已确认 |
| W7 | 迭代 / 聚合 / 条件节点折叠为普通控制流（`for` 循环、赋值/三元、`if/elif/else`） | §6.2 | 已确认 |
| W8 | 图节点/边数将少于 293/324 | §6.7 | 已确认（具体数字见 §3） |

## 2. 未知运行语义（DSL 无法确认）

### 2.1 外部接口与协议

> **用户 2026-09-23 决定：以下仍无外部结论的问题，一律按「缺省采用」执行**（即右列的缺省处理即为实现依据）。状态列保留「待确认」以标记结论来源不是外部输入，避免日后误认为是已核实的协议。

| 编号 | 问题 | 状态 | 缺省处理 |
|---|---|---|---|
| Q1 | 回写接口 `POST /callback/update-profile` 的成功判定协议？HTTP 200 是否等同业务成功？ | **缺省采用**（阻塞 V0.3） | 只记 HTTP 状态与响应体，标记「回写状态待确认」，不判定业务成功 |
| Q2 | 回写接口是否支持幂等键？粒度为何？ | **缺省采用**（阻塞 V0.3） | 不自动重试 POST，超时一律标记待确认 |
| Q5 | HTTP 的 `timeout: 0`、`retry_interval`（无单位）、`max_retries: 3` 的实际运行语义 | **缺省采用**（阻塞 V0.2） | 采用显式配置默认值（连接 10s / 读 600s、间隔 500ms），见 `.env.example` §5。**不得把 DSL 原值直接传给 HTTP 库** |
| Q6 | 语音服务轮询的终止条件与最大轮询次数（`poll_count` 只被提取未被使用） | **缺省采用**（阻塞 V0.3） | 按有限次数上限实现，上限值标注待确认 |
| Q7 | 现有调用方协议：同步/异步、超时、鉴权、字段结构 | **缺省采用**（阻塞 V0.5） | 先只提供异步提交 + 查询 run_id |
| Q8 | 峰值请求率、单任务模型调用数与 token 量、期望时延、留痕保留期 | **缺省采用**（阻塞 V0.5） | 先设保守默认值，配置化 |
| ~~Q4~~ | ~~`llm_model` 合法取值~~ | **已关闭** | 由 §6.4.5 解决（参数废弃） |
| ~~Q3~~ | ~~入口图双节点是否并行重复调用~~ | **已确认** | 只保留一个节点（见 W1） |
| ~~Q9~~ | ~~回放验证的输入来源~~ | **已确认** | `phone_numbers.txt`（实测非空，见 D2） |

### 2.2 Dify 平台语义（本轮新增）

| 编号 | 问题 | 状态 | 缺省处理 |
|---|---|---|---|
| P1 | `iteration` 节点的 `parallel_nums: 10` 与 `is_parallel: false` 组合语义 | **待确认** | **按串行实现**（与文档 §1.3-2「默认串行、逐项」一致）。`is_parallel: false` 是权威信号，`parallel_nums: 10` 在其下不生效 |
| P2 | `iteration` 的 `error_handle_mode: terminated` 派生行为：某一项失败是否终止整轮并阻断下游？ | **待确认** | 按「失败传播、终止迭代」实现；下游与最终回写被阻断，保留已完成结果 |
| P3 | `variable-aggregator` 开启 `group_enabled` 时的取值行为（多组并存的输出形态） | **待确认** | 返回一个普通对象，下游按属性取（`hobby` / `consumption`）。实测下游 `code` 节点确实取 `["output"]`，与「分组聚合 → 普通对象」一致 |
| P4 | `if-else` 两侧分支汇聚到同一 `variable-aggregator` 时的取值行为（非教科书式二选一，出现在人设特征判断与各截图类工作流） | **待确认** | 按 aggregator 语义「取第一个可用输入」实现；因两支互斥，实际不会同时产出 |
| P5 | HTTP 非 2xx 是否使节点失败？插件默认参数为何？ | **待确认** | 与 Q5 合并处理，走显式配置 |

### 2.3 模型与提示词

| 编号 | 问题 | 状态 | 缺省处理 |
|---|---|---|---|
| M1 | 「3 次尝试」是指最多 3 次（首次 + 2 重试），还是首次 + 3 次重试 = 4 次？ | **待确认** | 按**最多 3 次尝试**实现（§6.4.3） |
| M2 | 17 处 `tool` 节点的 `output_json` 入参**全部为 `false`**，无法据此判断任务是否需要 JSON 输出 | **已确认（事实）** | 按「下游消费方语义」逐个确认：下游 `code` 对其输出做 JSON 解析、或提示词明确约定输出 JSON，即 `expect_json=True`。须在 V0.3 逐点标注 |
| M3 | 全项目 12 个 vision 节点要求统一模型具备多模态能力 | **已确认（硬约束）** | `.env` 的 `LLM_MODEL` 选型时必须确认；实测 12 个节点全部位于 6 个截图解析工作流，每流 2 个 |

## 3. 双套计数

### 3.1 原 DSL 计数（实测，16 项断言全部命中）

| 项 | 数量 |
|---|---|
| 工作流 | 19 |
| 节点 | 293 |
| 边 | 324 |
| `tool` 节点 / 其中 `gemini_retry_2_times` | 38 / 17 |
| `llm` 节点 / 其中 vision | 35 / 12 |
| `code` | 49 |
| `template-transform` | 46 |
| `if-else` | 26 |
| `variable-aggregator` / 其中分组 | 23 / 1 |
| `end` | 22 |
| `start` | 19 |
| `http-request` | 19 |
| `iteration` / `iteration-start` | 8 / 8 |
| 绑定条目（见下方口径说明） | 329 |
| DSL 文本中 `{{#node.field#}}` 出现次数 | 111 |
| 顺序依赖但无数据消费的边 | 145 |
| 提示词/模板节点数 | 81（46 + 35） |

> **口径说明（易错点）**：「绑定条目」与「`{{#...#}}` 出现次数」是两个不同的数，不可混用。绑定条目包含节点的 `variables[]` 声明、`tool` 节点的每个参数、条件分支的变量选择器——即使其值不是引用字面量也算一条。DSL 文本中 `{{#...#}}` 的实际出现次数为 111。两者均记录在 `variable_ledger.json`。

### 3.2 §6 优化后的 Python 实现计数（**规划预算值，非已实现验证值**）

以下为按 §6 规则推算的预算，**尚未实现、未经验证**，V0.3 实现后须以实测替换：

| 差异来源 | 影响 |
|---|---|
| Q3 决策（W1） | −1 节点、−2 边 |
| 17 处 `gemini` 归一（W2/W3） | −17 个 `tool` 节点；`Gemini（异常输出重试版）` 子流程的 11 节点不再作为独立图存在 |
| 4 处 `llm_model` 分支归一（W4） | −4 个 `if-else` 节点；8 个工作流的 `start` 各减少 1 个入参 |
| 迭代/聚合/条件折叠（W7） | `iteration` 8、`iteration-start` 8、`variable-aggregator` 23、`if-else` 26 全部改为普通控制流 |
| `template-transform` 合并（§6.5） | 46 个节点可合并为同一模块的顺序代码；**提示词正文仍独立存放** |

**§3.2 不给出单一总数**：折叠后的「节点」不再是同一维度的事物（一个 `for` 循环替代 2 个节点），给出单一数字会制造虚假精度。**唯一有意义的对照基准是 §3.1 的 DSL 计数**，V0.3 实现时逐项建立「DSL 节点 → Python 函数」的对应关系并单独出具。

## 4. 已决定事项（用户 2026-09-23 答复）

| 编号 | 事项 | 决定 | 落地情况 |
|---|---|---|---|
| D5 | 仓库根目录受控文件 `Untitled` 是否删除 | **删除** | 已删除（`git rm --cached` + 删文件）。确认其内容仅一行 `dify_dsl_data`、无引用方 |
| D6 | `docs/specs/` 缺 `README.md`；`docs/architecture.md` 为空 | `architecture.md` **待后续完成代码架构设计开发后编写**；`specs/` 目录专门存放各类设计文档 | 本版本不新增 `specs/README.md`；`architecture.md` 保持为空，留待架构设计阶段 |
| D7 | 密钥轮换时机 | **在完成 V0 版本后手工执行，V0 开发阶段暂不考虑** | `secrets_report.md` §3 的「V0.3 前」建议已作废，改为 V0 完成后手工轮换。V0 全程不触达生产外网，风险窗口可控 |
| D8 | 提示词正文是否入库 | **需要入库**，是后续版本的重要优化目标 | 已由 `data/prompts/`（gitignored）迁至 **`docs/specs/prompts/`（入库）**，81 份。入库前已扫描确认不含密钥与手机号，`verify_ledger.py` 持续校验 |

### 4.1 其他待决问题的处置

用户指示：**其余待决策的 Qx 问题按缺省方案实现**。据此，§2.1 的 Q1、Q2、Q5–Q8 的状态改为「缺省采用」，右列缺省处理即为实现依据。

**注意区分**：该指示覆盖的是 **Qx 系列**（外部接口与协议）。§2.2 的 P1–P5（Dify 平台语义）与 §2.3 的 M1（重试次数口径）**不在本次指示范围内**，仍为待确认，按各自缺省处理推进，但须在 V0.2/V0.3 实现时以 Dify 实际行为或执行记录复核。

### 4.2 本清单的后续维护

- 本文件是 **v1**。Q1/Q2/Q5–Q8 的「缺省采用」是**实现依据，不是等价验证通过**——缺外部协议时不得宣称等价。
- 任一 Qx 获得外部结论后，须回来更新本表并把状态改为「已确认」，同时修订实现。
