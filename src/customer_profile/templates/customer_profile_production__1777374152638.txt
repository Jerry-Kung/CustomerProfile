**任务背景**：
我是一名汽车销售顾问，刚刚认识了一位可能有购车意向的潜在客户。我从公开的合法渠道获取了该客户的一些信息（例如微信主页，朋友圈截图等），并为这位客户构建了一幅“个人特征画像”。现在，我需要基于客户的画像数据，为该客户生成一些人设标签，用于帮助其他销售人员能够快速了解这位客户。

**任务目标及要求**：
我已经提前建立了一个包含100多个标签的标签库，你应该优选从标签库中选择与客户最贴合的标签。若标签库中没有特别合适的标签，你可以自行生成合适的标签，但风格请与标签库中的标签保持一致。

选择标签时请注意：
- 标签库中标签等级为“精确”的标签，对客户的刻画精度最高，但选择错误时会对销售人员产生严重的误导。因此选择“精确”等级标签时需要尽量慎重，原则上每个选择的“精确”类别标签必须有相应的A级证据直接支撑；
- 如果客户信息量过于缺乏，无法为该客户精确画像。你可以优先从标签库中选择标签等级为“保守”的标签，这样一般不会带来较大的误导后果。

**任务输入**：
- 基于客户的公开信息整理的客户证据线索列表，格式如下：
{{ output }}

- 客户的特征画像（包含人设特征、兴趣爱好特征和消费性格特征三个部分），格式如下：
{{ output_1 }}

- 预提取的客户初始信息，均为真实信息，可以直接使用。包括以下内容：
1、客户的微信昵称（wechat_nickname）
2、客户性别
3、客户的微信号（wechat_id）

- 预置的初始标签库，包含100多个预置的初始标签。

**任务输出**：
请采用JSON格式输出你的标签列表，列表中包含10-20个标签。
JSON格式说明：
{
    "tag_items": [ // 10-20个标签
        {
            "tag_name": "标签名称",
            "tag_type": "标签类别",
            "tag_description": "标签详细描述，不要直接使用标签库中的描述，请根据当前客户的情况，重新生成最适合的标签描述",
            "evidence_support": "与该标签相关的证据项，把相关的证据文本整理总结后填写在这里，不要使用证据ID",
            "reasoning_content": "推理步骤简要描述，如何基于原始证据推理得到当前标签",
            "confidence": 0.9 // 推理置信度，0-1之间
        },
        ...
    ]
}

**注意事项**：
- 请使用简体中文输出。
- 请直接输出纯 JSON，不要使用 ```json 或 ``` 包裹，不要输出任何解释文字。请保证你的输出能够被json.loads()直接解析，不得包含任何多余的内容或字符，各Json字段的value内容请使用简体中文填写。

输入内容：
{{ output_2 }}
【预提取的客户初始信息】：
{{ result }}
[初始标签库]：
{{ output_3 }}
请给出你的输出：